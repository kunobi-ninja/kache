[package]
name = "kache-proofs"
version = "0.16.1"
publish = false
edition = "2024"
description = "Repository-only formal verification harnesses for kache."
license = "Apache-2.0"
repository = "https://github.com/kunobi-ninja/kache"
rust-version = "1.93"

[package.metadata.kache]
coverage-owner = "kani"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }
