# Workspace shared by two SIMULTANEOUS cargo invocations in
# parallel-build.sh. The `[workspace]` table also stops cargo walking up
# into the kache repo's own workspace — see multi-dep.
[workspace]
members = ["heavy1", "heavy2", "heavy3", "app"]
resolver = "2"
